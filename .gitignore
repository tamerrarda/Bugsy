node_modules/
dist/
.env
.env.local
*.local

# Private signing key for the stable extension ID. The *public* half lives in
# manifest.json ("key"); this file must never be committed or shipped.
.keys/


# Local working notes, not part of the project.
CLAUDE.md
BUGSY_SPEC.md

.DS_Store
.env.production

# Store upload artifact, built from extension/dist
store/*.zip

# Challenge answers — kept private even though the repo is public.
# The scored bugLine + explanation live only in Postgres; publishing them here
# would let anyone map challenge id -> answer and top the leaderboard.
content/
drivers/
